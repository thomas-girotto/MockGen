<#@ template debug="false" hostspecific="false" language="C#" #>
<#@ assembly name="System.Core" #>
<#@ import namespace="System.Linq" #>
<#@ import namespace="System.Text" #>
<#@ import namespace="System.Collections.Generic" #>
using MockGen.Setup;
<#foreach (var ns in Descriptor.Namespaces) { #>
using <#=ns#>;
<#}#>

namespace MockGen
{
	internal class <#=Descriptor.TypeToMock#>MethodsSetup
	{
<# foreach (var property in Descriptor.Properties) {#>
        internal PropertySetup<<#=property.Type#>> <#=property.Name#>Setup { get; } = new PropertySetup<<#=property.Type#>>();		
<# } #>
<# foreach (var method in Descriptor.Methods) {#>
        internal <#=method.MethodSetupWithTypedParameters#> <#=method.UniqueName#>Setup { get; } = new <#=method.MethodSetupWithTypedParameters#>();
<#}#>

		internal void SetupDone()
		{
<# foreach (var property in Descriptor.Properties) {#>
            <#=property.Name#>Setup.SetupDone();
<# } #>
<# foreach (var method in Descriptor.Methods) {#>
            <#=method.UniqueName#>Setup.SetupDone();
<# } #>
		}
	}
}